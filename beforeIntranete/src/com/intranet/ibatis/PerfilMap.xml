<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="perfil">

	<select id="traePerfil" parameterType="com.intranet.bean.Usuario"
		resultType="com.intranet.bean.Perfil">
		select p.idPerfil,p.descPerfil
		from usuario u join perfil p
		on u.idPerfil=p.idPerfil
		where u.user = #{user} and
		u.password=#{password}
	</select>
</mapper>
